import MaterialIcons from "@expo/vector-icons/MaterialIcons";
import { router, useLocalSearchParams } from "expo-router";
import { useState } from "react";
import { Pressable, StyleSheet, Text, View } from "react-native";

import { Avatar } from "@/components/chat-ui";
import { ScreenContainer } from "@/components/screen-container";
import { useColors } from "@/hooks/use-colors";

export default function CallScreen() {
  const colors = useColors(); const { title = "Contact", mode = "video" } = useLocalSearchParams<{ id: string; title?: string; mode?: "voice" | "video" }>(); const [muted, setMuted] = useState(false); const [speaker, setSpeaker] = useState(true); const [video, setVideo] = useState(mode === "video");
  const control = (active: boolean) => ({ backgroundColor: active ? colors.primary : colors.elevated, borderColor: active ? colors.primary : colors.border }); const controlText = (active: boolean) => active ? colors.onPrimary : colors.foreground;
  return <ScreenContainer edges={["top", "bottom", "left", "right"]}><View style={[styles.page, { backgroundColor: colors.background }]}><View style={styles.top}><Text style={[styles.callType, { color: colors.primary }]}>{video ? "VIDEO CALL" : "VOICE CALL"}</Text><Text style={[styles.connection, { color: colors.muted }]}>Call signaling is not configured yet</Text></View><View style={styles.contact}><Avatar profile={{ display_name: title, avatar_path: null }} size={112} /><Text style={[styles.name, { color: colors.foreground }]}>{title}</Text><Text style={[styles.status, { color: colors.muted }]}>Ready to call when your call provider is connected</Text></View><View style={styles.controls}><Pressable onPress={() => setMuted((value) => !value)} style={[styles.control, control(muted)]}><MaterialIcons name={muted ? "mic-off" : "mic"} size={25} color={controlText(muted)} /><Text style={[styles.controlText, { color: controlText(muted) }]}>{muted ? "Unmute" : "Mute"}</Text></Pressable><Pressable onPress={() => setSpeaker((value) => !value)} style={[styles.control, control(speaker)]}><MaterialIcons name="volume-up" size={25} color={controlText(speaker)} /><Text style={[styles.controlText, { color: controlText(speaker) }]}>Speaker</Text></Pressable><Pressable onPress={() => setVideo((value) => !value)} style={[styles.control, control(video)]}><MaterialIcons name={video ? "videocam" : "videocam-off"} size={25} color={controlText(video)} /><Text style={[styles.controlText, { color: controlText(video) }]}>Video</Text></Pressable><Pressable onPress={() => router.back()} style={[styles.control, { backgroundColor: colors.error, borderColor: colors.error }]}><MaterialIcons name="call-end" size={25} color={colors.onError} /><Text style={[styles.controlText, { color: colors.onError }]}>End</Text></Pressable></View></View></ScreenContainer>;
}
const styles = StyleSheet.create({ page: { flex: 1, justifyContent: "space-between", padding: 24 }, top: { alignItems: "center", paddingTop: 20 }, callType: { fontSize: 12, fontWeight: "900", letterSpacing: 1.5 }, connection: { fontSize: 13, marginTop: 7 }, contact: { alignItems: "center", gap: 10 }, name: { fontSize: 26, fontWeight: "900", marginTop: 7 }, status: { fontSize: 14, textAlign: "center" }, controls: { flexDirection: "row", justifyContent: "space-between", paddingBottom: 16 }, control: { alignItems: "center", borderRadius: 18, borderWidth: 1, gap: 6, height: 68, justifyContent: "center", width: 68 }, controlText: { fontSize: 10, fontWeight: "800" } });
